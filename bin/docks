#!/bin/sh
CURR_PATH=$(pwd)
DOCKERFILE="/home/aorith/githome/odroid/docker/"
cd $DOCKERFILE

if [ "$1" = "stop" ]
then
    docker stop cardigann
    docker stop jackett
    docker stop sonarr
    docker stop radarr
    docker stop lidarr
elif [ "$1" = "start" ]
then
    #docker-compose up -d cardigann
    docker-compose up -d jackett
    docker-compose up -d sonarr
    docker-compose up -d radarr
    #docker-compose up -d lidarr
else
    echo "Usage: $(basename $0) stop|start"
    exit 1
fi

cd $CURR_PATH
